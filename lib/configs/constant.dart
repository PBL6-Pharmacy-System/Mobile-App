import 'package:flutter/material.dart';
import 'package:pharmacy_app/configs/gap.dart';

// 🎨 Màu xanh pha trắng hiện đại - Pharmacy App
const Color primaryColor = Color(0xFF4A90D9); // Xanh dương nhạt pha trắng
const Color primaryLightColor = Color(0xFF7EB3F1); // Xanh nhạt hơn
const Color primaryDarkColor = Color(0xFF2E6DB3); // Xanh đậm hơn cho contrast
const Color accentColor = Color(0xFF5BC0EB); // Xanh cyan nhạt
const Color appColor = Color(0xFFE8F4FD); // Xanh rất nhạt cho background
const Color secondaryColor = Color(0xFF6B7B8C); // Xám xanh
const Color shadowColor = Color(0xFFB8C5D3); // Xám xanh nhạt
const Color greyColor = Color(0xFF8E99A8); // Xám trung tính
const Color borderColor = Color(0xFFD1DBE6); // Xám xanh border
const Color textColor = Color(0xFF2D3748); // Xám đậm cho text
const Color textSecondary = Color(0xFF718096); // Xám cho text phụ

const EdgeInsets paddingApp = EdgeInsets.symmetric(
  horizontal: Gap.md,
  vertical: Gap.lX,
);

// Key local storate
const String kNotification = "key_notification";
